
function rand(min, max) {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min + 1) + min);
}

let suma = 0;

suma = 0.1 + 0.2

// suma = suma + 5;

console.log(suma.toFixed(2)); // apvalinimas 
